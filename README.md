# Sklads - Система управления складами

React-приложение для управления складами, организациями, номенклатурами и складскими операциями.

## 📁 Структура проекта

```
uhack_react/
├── public/                 # Статические файлы
│   └── assets/            # Изображения и ресурсы
├── src/
│   ├── components/        # React компоненты
│   │   ├── auth/         # Компоненты аутентификации
│   │   ├── barcode/      # Сканер штрихкодов
│   │   ├── dialogs/      # Модальные окна
│   │   ├── organization/ # Компоненты организаций
│   │   ├── stock/        # Компоненты складских операций
│   │   └── warehouse/    # Компоненты складов
│   ├── context/          # React Context провайдеры
│   ├── pages/            # Страницы приложения
│   ├── services/         # API сервисы
│   ├── theme/            # Система тем
│   ├── ui/               # UI компоненты
│   ├── App.jsx           # Главный компонент с роутингом
│   └── index.js          # Точка входа
└── package.json
```

## 🏗️ Архитектура приложения

### Точка входа (`src/index.js`)
- Инициализирует React приложение
- Подключает глобальные стили
- Рендерит корневой компонент `App`

### Главный компонент (`src/App.jsx`)
Центральный компонент, который:
- Настраивает роутинг через `react-router-dom`
- Оборачивает приложение в провайдеры контекста:
  - `ThemeProvider` - управление темой (светлая/темная)
  - `AuthProvider` - управление аутентификацией
- Определяет все маршруты приложения
- Обрабатывает защищенные и публичные маршруты

## 📦 Основные компоненты

### 1. Аутентификация (`src/components/auth/`)

#### `LoginPage/LoginPage.jsx`
- Страница входа в систему
- Обрабатывает токены приглашения из URL (`?invite=token`)
- Сохраняет токен приглашения в `localStorage`
- Поддерживает темную тему

#### `RegisterPage/RegisterPage.jsx`
- Страница регистрации
- Аналогично обрабатывает токены приглашения
- Валидация формы регистрации

#### `VerificationPage/VerificationPage.jsx`
- Подтверждение email адреса
- Ввод кода подтверждения

### 2. Сканер штрихкодов (`src/components/barcode/`)

#### `BarcodeScanner.jsx`
- Компонент для сканирования/ввода штрихкодов
- Интегрирован с API поиска номенклатур
- При отсутствии товара предлагает создать новый
- При наличии товара открывает форму создания операции

### 3. Диалоги (`src/components/dialogs/`)

Все модальные окна для создания и редактирования:

- `CreateNomenclatureDialog.jsx` - создание номенклатуры
- `CreateOrganizationDialog.jsx` - создание организации
- `CreateWarehouseDialog.jsx` - создание склада
- `EditOrganizationDialog.jsx` - редактирование организации
- `EditWarehouseDialog.jsx` - редактирование склада
- `InviteMemberDialog.jsx` - приглашение участника
- `InventoryReportDialog.jsx` - отчет по инвентарю
- `QrCodeDialog.jsx` - отображение QR-кода организации

### 4. Организации (`src/components/organization/`)

#### `pages/OrganizationPage.jsx`
Главная страница организации, отображает:
- Информацию об организации
- Список складов
- Список участников
- Действия: создание склада, приглашение участников

#### `hooks/`
Кастомные хуки для работы с данными:
- `useOrganizationData.js` - загрузка данных организации
- `useWarehouses.js` - управление складами
- `useMembers.js` - управление участниками
- `useNomenclatures.js` - работа с номенклатурами

#### `styles/`
Модульные SCSS стили:
- `_variables.scss` - переменные (цвета, отступы)
- `_header.scss` - стили заголовков
- `_sections.scss` - стили секций
- `_buttons.scss` - стили кнопок
- `_tables.scss` - стили таблиц
- `_common.scss` - общие стили (loading, error, snackbar)
- `_warehouses.scss` - стили карточек складов

### 5. Склады (`src/components/warehouse/`)

#### `WarehousePage.jsx`
Страница склада с функционалом:
- Отображение информации о складе
- Сканер штрихкодов
- Список номенклатур склада
- **Список документов склада** (новое)
- Создание операций со складом

**Документы склада:**
- Отображение всех документов (приходные, отходные, инвентаризация)
- Просмотр и удаление документов
- Цветовая индикация типов документов

### 6. Складские операции (`src/components/stock/`)

#### `pages/StockOperationsPage.jsx`
Страница управления операциями:
- Просмотр всех операций
- Фильтрация по типам, складам, номенклатурам
- Создание новых операций

#### `hooks/useStockOperations.js`
Хук для работы с операциями:
- Загрузка операций с фильтрами
- Создание операций
- Обновление и удаление операций

#### `dialogs/CreateOperationForm.jsx`
Форма создания операции:
- Выбор типа операции (приход, расход, перемещение, корректировка)
- Выбор складов и номенклатур
- Ввод количества и комментария

#### `constants/operationTypes.js`
Константы типов операций

#### `utils/formatters.js`
Утилиты для форматирования данных операций

## 🎨 Система тем

### `src/theme/ThemeContext.jsx`
Глобальный провайдер темы:
- Управление светлой/темной темой
- Сохранение выбора в `localStorage`
- Применение класса `dark-mode` к `body`
- Предоставляет `useTheme()` хук для компонентов

### `src/theme/color.js`
Цветовые схемы для светлой и темной темы:
```javascript
colors = {
  light: { PAGE_BG, CARD, BTN, ... },
  dark: { PAGE_BG, CARD, BTN, ... }
}
```

### Использование темы в компонентах:
```javascript
import { useTheme } from '../../theme/ThemeContext';

const MyComponent = () => {
  const { isDark, toggleTheme, theme } = useTheme();
  
  return (
    <div className={`my-component ${isDark ? 'dark-mode' : ''}`}>
      <button onClick={toggleTheme}>Переключить тему</button>
    </div>
  );
};
```

## 🔐 Аутентификация и контекст

### `src/context/AuthContext.js`
Провайдер аутентификации:
- Управление состоянием пользователя
- Методы: `login()`, `register()`, `logout()`
- Автоматическая проверка токена при загрузке
- Обработка токенов приглашения

### `src/services/authService.js`
Сервис аутентификации:
- Работа с JWT токенами
- Сохранение/получение токена из `localStorage`
- Проверка валидности токена
- Получение данных текущего пользователя

## 🌐 API сервисы

### `src/services/api.js`
Централизованный класс `ApiService` для всех API запросов:

#### Методы аутентификации:
- `login(credentials, inviteToken?)` - вход
- `register(userData, inviteToken?)` - регистрация
- `verifyEmail(email, code)` - подтверждение email
- `getMe()` - получение текущего пользователя

#### Методы организаций:
- `getOrganizations()` - список организаций
- `createOrganization(data)` - создание организации
- `getOrganization(id)` - получение организации
- `updateOrganization(id, data)` - обновление
- `deleteOrganization(id)` - удаление
- `getOrganizationWarehouses(orgId)` - склады организации
- `inviteMember(orgId, email)` - приглашение участника

#### Методы складов:
- `getWarehouses()` - список складов
- `getWarehouse(id)` - получение склада
- `createWarehouse(data)` - создание склада
- `updateWarehouse(id, data)` - обновление
- `deleteWarehouse(id)` - удаление

#### Методы номенклатур:
- `getNomenclatures(warehouseId?, skip?, limit?)` - список
- `createNomenclature(data)` - создание
- `searchByBarcode(barcode)` - поиск по штрихкоду

#### Методы операций:
- `createStockOperation(data)` - создание операции
- `getStockOperations(params)` - список с фильтрами
- `getStockOperation(id)` - получение операции

#### Методы документов склада (новое):
- `createSkladDocument(data)` - создание документа
- `getSkladDocuments(skladId?)` - список документов
- `getSkladDocument(docId)` - получение документа
- `updateSkladDocument(docId, data)` - обновление
- `deleteSkladDocument(docId)` - удаление
- `addDocumentItem(docId, data)` - добавление номенклатуры
- `getDocumentItems(docId)` - список номенклатур документа
- `updateDocumentItem(itemId, data)` - обновление номенклатуры
- `deleteDocumentItem(itemId)` - удаление номенклатуры

**Особенности:**
- Автоматическое добавление JWT токена в заголовки
- Обработка ошибок
- Базовый URL: `https://rsue.devoriole.ru/api`

## 🛣️ Маршрутизация

### Публичные маршруты:
- `/login` - страница входа
- `/register` - страница регистрации
- `/verify` - подтверждение email
- `/auth` - редирект на `/login` с сохранением `?invite=token`
- `/orga/join/:code` - присоединение к организации по коду

### Защищенные маршруты (требуют авторизации):
- `/dashboard` - главная страница со списком организаций
- `/organizations/:id` - страница организации
- `/warehouses/:id` - страница склада
- `/stock/operations` - страница операций
- `/organizations/:id/operations` - операции организации

### Логика защиты:
```javascript
element={user ? <ProtectedPage /> : <Navigate to="/login" replace />}
```

## 📄 Страницы

### `src/pages/Dashboard.jsx`
Главная страница:
- Отображение списка организаций пользователя
- Создание новых организаций
- Информация об аккаунте
- Переход к выбранной организации

### `src/pages/JoinOrganizationPage.jsx`
Страница присоединения к организации по QR-коду или коду

## 🎨 Стилизация

### Подход к стилям:
1. **Модульные SCSS файлы** - каждый компонент имеет свой `.scss` файл
2. **Переменные** - общие переменные в `_variables.scss`
3. **Темная тема** - поддержка через класс `.dark-mode`
4. **Адаптивность** - медиа-запросы для мобильных устройств

### Структура стилей:
```scss
.component-name {
  // Базовые стили
  
  &.dark-mode {
    // Стили для темной темы
  }
  
  @media (max-width: 768px) {
    // Мобильные стили
  }
}
```

### Глобальные стили:
- `src/index.scss` - базовые стили для `html`, `body`
- `src/App.scss` - стили для корневого компонента

## 🔧 Утилиты

### `src/components/utils/jwt.js`
Утилиты для работы с JWT токенами:
- Декодирование токена
- Проверка срока действия
- Извлечение данных пользователя

## 📱 Основные функции

### 1. Управление организациями
- Создание, редактирование, удаление
- Управление участниками
- Приглашение по email или QR-коду

### 2. Управление складами
- Создание складов в организации
- Просмотр информации о складе
- Управление номенклатурами

### 3. Сканирование штрихкодов
- Поиск товаров по штрихкоду
- Автоматическое создание номенклатуры при отсутствии
- Создание операций с найденным товаром

### 4. Складские операции
- Приход товара
- Расход товара
- Перемещение между складами
- Корректировка остатков

### 5. Документы склада
- Просмотр документов (приходные, отходные, инвентаризация)
- Удаление документов
- Цветовая индикация типов

## 🔄 Потоки данных

### Аутентификация:
1. Пользователь вводит credentials
2. `AuthContext.login()` вызывает `authService.login()`
3. `authService` использует `apiService` для запроса
4. Токен сохраняется в `localStorage`
5. Пользователь перенаправляется на `/dashboard`

### Загрузка данных:
1. Компонент использует `useEffect` для загрузки
2. Вызывает метод `apiService` (например, `getOrganizations()`)
3. `apiService` добавляет токен и делает запрос
4. Данные сохраняются в состояние компонента
5. Компонент перерисовывается с новыми данными

### Обработка ошибок:
- API ошибки обрабатываются в `apiService.request()`
- Компоненты показывают уведомления через `showSnackbar()`
- Ошибки авторизации перенаправляют на `/login`

## 🎯 Ключевые паттерны

### 1. Custom Hooks
Используются для переиспользования логики:
- `useOrganizationData` - загрузка данных организации
- `useStockOperations` - работа с операциями
- `useTheme` - управление темой

### 2. Context API
Для глобального состояния:
- `AuthContext` - аутентификация
- `ThemeContext` - тема приложения

### 3. Композиция компонентов
Компоненты разбиты на мелкие переиспользуемые части:
- Диалоги вынесены в отдельные компоненты
- Стили модульные и переиспользуемые

### 4. Обработка состояний загрузки
Все компоненты показывают состояние загрузки:
```javascript
if (loading) return <div className="loading">Загрузка...</div>;
```

## 🚀 Запуск проекта

```bash
# Установка зависимостей
npm install

# Запуск в режиме разработки
npm start

# Сборка для продакшена
npm run build
```

## 📝 Заметки

- Все API запросы требуют JWT токен в заголовке `Authorization: Bearer {token}`
- Токен хранится в `localStorage` под ключом `authToken`
- Тема хранится в `localStorage` под ключом `theme`
- Токены приглашения сохраняются в `localStorage` под ключом `invite_token`
- Приложение использует React Router v7 для навигации
- Material-UI используется для некоторых компонентов (TextField, Button, Dialog)

## 🔐 Безопасность

- JWT токены автоматически добавляются в заголовки запросов
- Защищенные маршруты проверяют наличие пользователя
- Токены приглашения передаются при регистрации/логине
- Ошибки авторизации автоматически перенаправляют на страницу входа

## 🎨 Темная тема

Поддержка темной темы реализована через:
1. `ThemeContext` - глобальное управление темой
2. Класс `dark-mode` на `body` элементе
3. SCSS селекторы `.dark-mode &` для стилей
4. Изображения для переключателя темы в `public/assets/LoginPage/`

## 📚 Дополнительные ресурсы

- API документация: `https://rsue.devoriole.ru/api/docs`
- Базовый URL API: `https://rsue.devoriole.ru/api`
- React Router: https://reactrouter.com/
- Material-UI: https://mui.com/
